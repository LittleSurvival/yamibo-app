CREATE TABLE AppSyncRecoveryPayload (
    sessionId TEXT NOT NULL PRIMARY KEY REFERENCES AppSyncRecoverySession(sessionId),
    payloadKind TEXT NOT NULL,
    payloadIdentity TEXT NOT NULL,
    canonicalEnvelope TEXT NOT NULL,
    envelopeFingerprint TEXT NOT NULL
);
